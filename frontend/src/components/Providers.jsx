'use client';

import { ReactNode } from 'react';
import { Toaster } from 'sonner';

export function Providers({ children }: { children) {
  return (
    <>
      {children}
      <Toaster 
        position="top-center" 
        toastOptions={{
          style);
}
